"""Delivery-Agent — релиз-менеджер контура производства.

Модуль отдельного репозитория, который Harness (`po-helper-org/poh-issue-agents`)
подключает к своему воркеру. Здесь живут воркфлоу релиза, его правила и порты;
в Harness — только точка подключения и реализации портов поверх его GitHub-клиента.

Слои разведены намеренно:

- `rules`, `render`, `model` — чистые: ни сети, ни Temporal, ни GitHub. Их
  тестируют без окружения, и именно они решают, ЧТО отгружать и в каком порядке.
- `ports` — протоколы, которыми воркфлоу разговаривает с внешним миром.
- `github`, `prod` — реализации портов по умолчанию (REST и docker).
- `activities`, `workflow` — обвязка Temporal.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
